[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "iapws95"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = ["matplotlib", "numpy"]

[tool.maturin]
features = ["python"]
